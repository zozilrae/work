# <항목 이름> — <한 줄 설명>

<!--
새 항목을 만들 때 이 파일을 채운다. 자리표시(<...>)와 이 주석은 지운다.
쓸 내용이 없는 절은 통째로 지운다. 빈 제목만 남겨 두지 않는다.
-->

<이 항목이 무엇을 만드는 곳인지 두세 문장. 산출물이 무엇이고 어디에 쌓이는지.>

- `<항목>/<폴더>/` — <무엇이 들어가는지>
- `<항목>/README.md` — <무엇을 적어 두는지. 갱신이 필요한 시점도 함께.>

## 자주 쓰는 명령

<저장소 루트에서 실행하는 형태로 적는다. 빌드·실행·테스트·산출물 생성 중 실제로 쓰는 것만.>

```bash
<명령>        # <무엇을 하는지>
```

의존성: <설치가 필요한 것. 이 환경에 이미 있는 것이라면 그렇게 적는다.>

## 이 항목의 규약

<파일을 건드리기 전에 알아야 하는 것만 적는다. 일반적인 개발 상식은 적지 않는다.>

- <형식·명명 규칙>
- <도구가 파싱하는 구조가 있다면 그 형식>
- <쉽게 밟는 함정과 그 이유>

## 구조

<여러 파일을 읽어야 파악되는 흐름만 적는다. 파일 목록 나열은 하지 않는다.>
