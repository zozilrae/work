# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 저장소 구조 — 항목별 작업 공간

여러 항목(item)을 담는 저장소다. **최상위 폴더 하나가 항목 하나**이고, 항목은 서로 독립적이다. 공통 빌드·테스트·린트 설정은 없다 — 필요한 도구는 항목 안에 둔다.

| 항목 | 내용 |
|---|---|
| `reports/` | 한국어 기술 조사 보고서와 PDF 변환 도구 |
| `presentations/` | HTML 기반 PPT 생성 (콘텐츠 분석, 구조 설계, 자동 슬라이드 생성) |
| `research-agent/` | Claude AI 기반 웹 리서치 자동화 (정보 수집, 분석, 리포트 생성) |
| `_template/` | 새 항목을 만들 때 복사하는 뼈대. 항목이 아니다. |

각 항목 폴더에는 그 항목의 `CLAUDE.md` 와 `README.md` 가 있다. **항목 안에서 작업할 때는 그 항목의 `CLAUDE.md` 가 실제 규약이다** — Claude Code가 해당 폴더의 파일을 다룰 때 자동으로 읽는다. 이 루트 파일은 저장소 전체에 걸친 규칙만 담는다.

### 항목 사이의 경계

- 한 항목의 파일을 다른 항목에서 직접 참조하지 않는다. 같은 도구가 두 항목에 필요하면 복사하거나, 그때 공용 항목으로 승격할지 논의한다.
- 커밋 하나는 한 항목 안에서 끝낸다. 여러 항목을 건드려야 하면 항목별로 나눠 커밋한다.
- 항목마다 쓰는 언어·도구·규약이 다를 수 있다. 루트에서 일괄 규칙을 강요하지 않는다.

## 새 항목 추가

```bash
cp -r _template <항목이름>        # 소문자, 단어 구분은 하이픈 (예: battery-sim)
```

그다음 세 가지를 반드시 한다.

1. `<항목이름>/CLAUDE.md` 의 자리표시를 채운다 — 무엇을 만드는 항목인지, 자주 쓰는 명령, 그 항목만의 규약.
2. `<항목이름>/README.md` 를 채운다.
3. **이 파일의 항목 표와 루트 `README.md` 의 항목 표에 한 줄을 추가한다.**

## 공통 규약

- 문서와 커밋 메시지는 한국어로 쓴다. 문서 본문은 경어 없이 `~이다 / ~한다` 평서체를 쓴다.
- 커밋 제목은 `<타입>(<항목>): 제목` 형식이다. 타입은 `docs` `feat` `fix` `chore` 를 쓰고, 항목은 최상위 폴더 이름이다.

  ```
  docs(reports): TR-2026-003 냉각 시스템 비교 보고서 추가
  fix(reports): md2pdf 표지 메타 파싱에서 빈 값 처리
  chore: 새 항목 뼈대 _template 추가          ← 저장소 전체 변경은 항목 생략
  ```

- 제목 한 줄 뒤에 본문으로 무엇이 들어갔는지 불릿으로 정리한다(기존 커밋 참조). 문서를 추가한 커밋은 해당 문서번호를 제목에 함께 적는다.
